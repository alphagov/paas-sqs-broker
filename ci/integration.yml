---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.13-stretch
inputs:
  - name: repo
params:
  ENABLE_INTEGRATION_TESTS: "true"
run:
  path: sh
  args:
    - -e
    - -c
    - |
      cd repo
      make test
